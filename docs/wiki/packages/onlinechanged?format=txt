= Onlinechanged =
You can send yourself an e-mail when your box comes online like this:

{{{
  online)
    mailer -s "Freetz online" -t <your e-mail address>
    ;;
}}}
For this to work, you should configure the [http://fritz.box/system/push.lua AVM Push service], but it is not necessary to enable it.