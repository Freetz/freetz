--- linux-2.6.39/.kernelvariables
+++ linux-2.6.39/.kernelvariables
@@ -35,7 +35,6 @@
 CP ?= /bin/cp
 export CP
 
-export KCONFIG_CONFIG = avm/conf/linux-2.6.x86
 
 # KCONFIG_NOTIMESTAMP is exported because it is included in $(kernelvariables_extra)
 override KCONFIG_NOTIMESTAMP := yes
