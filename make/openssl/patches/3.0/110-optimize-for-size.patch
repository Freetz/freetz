--- Configurations/10-main.conf
+++ Configurations/10-main.conf
@@ -1928,4 +1928,65 @@
         pointer_size     => "64",
     },
 
+    "linux-freetz-arm-asm" => {
+        inherit_from     => [ "linux-generic32" ],
+        CC               => "gcc",
+        CFLAGS           => "\$(FREETZ_MOD_OPTIMIZATION_FLAGS) -fomit-frame-pointer -Wall",
+        lib_cppflags     => add("-DL_ENDIAN"),
+        cppflags         => threads("-D_REENTRANT"),
+        ex_libs          => add("-ldl", threads("-pthread")),
+        bn_ops           => "BN_LLONG RC4_CHAR",
+        asm_arch         => 'armv4',
+        perlasm_scheme   => "linux32",
+        dso_scheme       => "dlfcn",
+        shared_target    => "linux-shared",
+        shared_cflag     => "-fPIC",
+        shared_extension => ".so.\$(SHLIB_VERSION_NUMBER)",
+    },
+    "linux-freetz-i686-asm" => {
+        inherit_from     => [ "linux-generic32" ],
+        CC               => "gcc",
+        CFLAGS           => "\$(FREETZ_MOD_OPTIMIZATION_FLAGS) -fomit-frame-pointer -Wall",
+        lib_cppflags     => add("-DL_ENDIAN"),
+        cppflags         => threads("-D_REENTRANT"),
+        ex_libs          => add("-ldl", threads("-pthread")),
+        bn_ops           => "BN_LLONG RC4_CHAR",
+        asm_arch         => 'x86',
+        perlasm_scheme   => "elf",
+        dso_scheme       => "dlfcn",
+        shared_target    => "linux-shared",
+        shared_cflag     => "-fPIC",
+        shared_extension => ".so.\$(SHLIB_VERSION_NUMBER)",
+    },
+    "linux-freetz-mips-asm" => {
+        inherit_from     => [ "linux-generic32" ],
+        CC               => "gcc",
+        CFLAGS           => "\$(FREETZ_MOD_OPTIMIZATION_FLAGS) -fomit-frame-pointer -Wall",
+        lib_cppflags     => add("-DB_ENDIAN"),
+        cppflags         => threads("-D_REENTRANT"),
+        ex_libs          => add("-ldl", threads("-pthread")),
+        bn_ops           => "BN_LLONG RC4_CHAR",
+        asm_arch         => 'mips32',
+        perlasm_scheme   => "o32",
+        dso_scheme       => "dlfcn",
+        shared_target    => "linux-shared",
+        shared_cflag     => "-fPIC",
+        shared_extension => ".so.\$(SHLIB_VERSION_NUMBER)",
+    },
+    "linux-freetz-mipsel-asm" => {
+        inherit_from     => [ "linux-generic32" ],
+        CC               => "gcc",
+        CFLAGS           => "\$(FREETZ_MOD_OPTIMIZATION_FLAGS) -fomit-frame-pointer -Wall",
+        lib_cppflags     => add("-DL_ENDIAN"),
+        cppflags         => threads("-D_REENTRANT"),
+        ex_libs          => add("-ldl", threads("-pthread")),
+        bn_ops           => "BN_LLONG RC4_CHAR",
+        asm_arch         => 'mips32',
+        perlasm_scheme   => "o32",
+        dso_scheme       => "dlfcn",
+        shared_target    => "linux-shared",
+        shared_cflag     => "-fPIC",
+        shared_extension => ".so.\$(SHLIB_VERSION_NUMBER)",
+    },
+
 );
