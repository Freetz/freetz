config DS_PACKAGE_PPTP
	bool "pptp 1.7.1"
	depends on DS_REPLACE_KERNEL
	select DS_PACKAGE_PPPD
	select DS_LIB_libutil
	default n
	help
		Linux and FreeBSD client for the proprietary Microsoft
		Point-to-Point tunneling protocol, PPTP, sponsored by HP.

