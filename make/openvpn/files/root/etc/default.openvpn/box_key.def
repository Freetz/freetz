CAPTION='Private Key (OpenVPN)'
DESCRIPTION='$(lang de:"Der private Schlüssel für diese Instanz. Wird nur für den Betrieb mit Zertifikaten benötigt." en:"The private key for this instance. Only needed if run with certficiates.")'
CONFIG_FILE='/tmp/flash/openvpn/box.key'
CONFIG_SAVE='modsave flash; chmod 600  ${CONFIG_FILE}; if [ ! -z "$(pidof openvpn)" ]; then echo ""; /mod/etc/init.d/rc.openvpn reload; fi'
CONFIG_TYPE='text'
