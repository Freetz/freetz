--- configure
+++ configure
@@ -9637,7 +9637,7 @@
 	{ echo "$as_me:$LINENO: checking for the linux kernel version" >&5
 echo $ECHO_N "checking for the linux kernel version... $ECHO_C" >&6; }
 
-	kernel=`uname -r`
+	kernel="2.6.x" # all Fritz!Boxes supported by freetz are based on kernel 2.6.x
 
 	case "${kernel}" in
 		2.6.*)
