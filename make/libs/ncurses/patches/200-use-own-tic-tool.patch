--- misc/run_tic.in
+++ misc/run_tic.in
@@ -88,6 +88,7 @@
 		exit 1
 	fi
 else
+	TIC_PATH="$FREETZ_BASE_DIR/tools/tic"
 	# Cross-compiling, so don't set PATH or run shlib.
 	SHLIB=
 	# reset $suffix, since it applies to the target, not the build platform.
