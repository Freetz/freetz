config EXTERNAL_FREETZ_LIB_libsodium
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libsodium
	bool "libsodium"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libsodium.so.1.0.0
