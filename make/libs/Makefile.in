ifeq ($(strip $(DS_TARGET_GXX)),y)
ifeq ($(strip $(DS_LIB_libuClibcxx)),y)
LIBS+=uclibcxx
endif
endif

ifeq ($(strip $(DS_LIB_libavmhmac)),y)
LIBS+=libavmhmac
endif

ifeq ($(strip $(DS_LIB_libbluetooth)),y)
LIBS+=bluez-libs
endif

ifeq ($(strip $(DS_LIB_libcapi20)),y)
LIBS+=libcapi
endif

ifeq ($(strip $(DS_LIB_libffi)),y)
LIBS+=libffi-sable
endif

ifeq ($(strip $(DS_LIB_libgpg_error)),y)
LIBS+=libgpg-error
endif

ifeq ($(strip $(DS_LIB_libjpeg)),y)
LIBS+=libjpeg
endif

ifeq ($(strip $(DS_LIB_libgcrypt)),y)
LIBS+=libgcrypt
endif

ifeq ($(strip $(DS_LIB_liblzo2)),y)
LIBS+=lzo
endif

ifeq ($(strip $(DS_LIB_libevent)),y)
LIBS+=libevent
endif

ifeq ($(strip $(DS_LIB_libmad)),y)
LIBS+=libmad
endif

ifeq ($(strip $(DS_LIB_libncurses)),y)
LIBS+=ncurses
endif

ifeq ($(strip $(DS_LIB_libterminfo)),y)
LIBS+=ncurses-terminfo
endif

ifeq ($(strip $(DS_LIB_libpcap)),y)
LIBS+=libpcap
endif

#ifeq ($(strip $(DS_LIB_libpjsip)),y)
#LIBS+=libpjsip
#endif

ifeq ($(strip $(DS_LIB_libcrypto)),y)
# libssl depends on libcrypto
LIBS+=openssl
endif

ifeq ($(strip $(DS_LIB_libz)),y)
LIBS+=zlib
endif

ifeq ($(strip $(DS_LIB_libreadline)),y)
LIBS+=readline
endif

ifeq ($(strip $(DS_LIB_libmatrixssl)),y)
LIBS+=matrixssl
endif

ifeq ($(strip $(DS_LIB_libxyssl)),y)
LIBS+=xyssl
endif

ifeq ($(strip $(DS_LIB_libusb)),y)
LIBS+=libusb
endif

ifeq ($(strip $(DS_LIB_libpng12)),y)
LIBS+=libpng
endif

ifeq ($(strip $(DS_LIB_libart_lgpl_2)),y)
LIBS+=libart_lgpl
endif

ifeq ($(strip $(DS_LIB_libfreetype)),y)
LIBS+=freetype
endif

ifeq ($(strip $(DS_LIB_libelf)),y)
LIBS+=libelf
endif

ifeq ($(strip $(DS_LIB_libglib)),y)
LIBS+=glib
endif

ifeq ($(strip $(DS_LIB_libglib2)),y)
LIBS+=glib2
endif

ifeq ($(strip $(DS_LIB_libintl)),y)
LIBS+=gettext
endif

ifeq ($(strip $(DS_LIB_libiconv)),y)
LIBS+=libiconv
endif

ifeq ($(strip $(DS_LIB_libftdi)),y)
LIBS+=libftdi
endif
