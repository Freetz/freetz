config EXTERNAL_FREETZ_LIB_libpcre2
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libpcre2
	bool "libpcre2"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libpcre2-8.so.0.11.1

config EXTERNAL_FREETZ_LIB_libpcre2_posix
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libpcre2_posix
	bool "libpcre2-posix"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libpcre2-posix.so.3.0.3

