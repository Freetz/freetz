config EXTERNAL_FREETZ_LIB_libslang
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libslang
	bool "libslang"
	default n
	help
		externals these file(s):
		 /usr/lib/freetz/libslang.so.2.2.4
		 /usr/lib/freetz/slang
