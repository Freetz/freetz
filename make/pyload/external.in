config EXTERNAL_FREETZ_PACKAGE_PYLOAD
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_PYLOAD && EXTERNAL_SUBDIRS
	bool "pyLoad"
	default n
	help
		externals the following file(s):
		 /opt/pyLoad/ (recursive)

