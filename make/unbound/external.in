if EXTERNAL_ENABLED

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_DAEMON
		depends on FREETZ_PACKAGE_UNBOUND_DAEMON
		bool "unbound"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_ANCHOR
		depends on FREETZ_PACKAGE_UNBOUND_ANCHOR
		bool "unbound-anchor"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound-anchor

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_CHECKCONF
		depends on FREETZ_PACKAGE_UNBOUND_CHECKCONF
		bool "unbound-checkconf"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound-checkconf

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_CONTROL
		depends on FREETZ_PACKAGE_UNBOUND_CONTROL
		bool "unbound-control"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound-control

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_CONTROL_SETUP
		depends on FREETZ_PACKAGE_UNBOUND_CONTROL_SETUP
		bool "unbound-control-setup"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound-control-setup

	config EXTERNAL_FREETZ_PACKAGE_UNBOUND_HOST
		depends on FREETZ_PACKAGE_UNBOUND_HOST
		bool "unbound-host"
		default n
		help
			externals the following file(s):
			 /usr/bin/unbound-host

endif # EXTERNAL_ENABLED

