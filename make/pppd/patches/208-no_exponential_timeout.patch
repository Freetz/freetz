--- pppd/plugins/rp-pppoe/discovery.c.orig	2008-11-26 11:02:29.000000000 +0100
+++ pppd/plugins/rp-pppoe/discovery.c	2009-11-05 11:08:32.000000000 +0100
@@ -563,7 +563,9 @@
 	sendPADR(conn);
 	conn->discoveryState = STATE_SENT_PADR;
 	waitForPADS(conn, timeout);
+#if 0
 	timeout *= 2;
+#endif
     } while (conn->discoveryState == STATE_SENT_PADR);
 
     /* We're done. */
