--- pppd/main.c.orig	2009-07-31 00:40:27.000000000 +0200
+++ pppd/main.c	2009-07-31 00:40:27.000000000 +0200
@@ -772,8 +772,7 @@
 	/* update pid files if they have been written already */
 	if (pidfilename[0])
 	    create_pidfile(pid);
-	if (linkpidfile[0])
-	    create_linkpidfile(pid);
+	create_linkpidfile(pid);
 	exit(0);		/* parent dies */
     }
     setsid();
