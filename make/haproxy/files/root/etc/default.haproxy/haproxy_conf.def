CAPTION="$(lang de:"Konfiguration" en:"Configuration")"
DESCRIPTION="$(lang de:"Lastverteiler und Proxy Konfiguration, haproxy typische Syntax" en:"Load balancing and proxying configuration, haproxy typical syntax")"

CONFIG_FILE='/tmp/flash/haproxy/haproxy.conf'
CONFIG_SAVE='modsave flash; if pidof haproxy > /dev/null; then echo ""; /mod/etc/init.d/rc.haproxy restart; fi'
CONFIG_TYPE='text'

TEXT_ROWS=25

