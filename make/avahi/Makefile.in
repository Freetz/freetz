ifneq (,$(strip $(filter y,$(FREETZ_LIB_libavahi_common) $(FREETZ_LIB_libavahi_core) $(FREETZ_LIB_libavahi_client))))
LIBS+=avahi
endif

ifeq ($(strip $(FREETZ_PACKAGE_AVAHI)),y)
PACKAGES+=avahi
endif
