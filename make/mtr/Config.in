config DS_PACKAGE_MTR
	bool "mtr 0.69"
	select DS_LIB_libresolv
	select DS_LIB_libncurses
	default n
	help
		mtr combines the functionality of the 'traceroute' and 'ping' programs
		in a single network diagnostic tool.
