--- configure
+++ configure
@@ -6377,6 +6377,7 @@
 fi
 
 
+if false; then
 # Find resolver library.
 ac_fn_c_check_func "$LINENO" "res_query" "ac_cv_func_res_query"
 if test "x$ac_cv_func_res_query" = xyes; then :
@@ -6516,6 +6517,8 @@
 
 fi
 
+fi
+
 if test "x" = "x$RESOLV_LIBS"; then :
 
   { $as_echo "$as_me:${as_lineno-$LINENO}: checking for dn_expand in -lresolv" >&5
