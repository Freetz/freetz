--- configure
+++ configure
@@ -4841,7 +4841,7 @@
 case "$host" in
 	*-*-linux-gnu)
 	       INCLUDES="${INCLUDES} -DLINUX"
-               LIBS="${LIBS} -lc"
+#              LIBS="${LIBS} -lc"
 	       ;;
 	*darwin*)
     	   	INCLUDES="${INCLUDES} -DLINUX -DOSX -isystem"
