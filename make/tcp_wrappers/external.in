config EXTERNAL_FREETZ_PACKAGE_TCP_WRAPPERS
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_TCP_WRAPPERS
	bool "tcp_wrappers"
	default n
	help
		externals these file(s):
		 /sbin/tcpd
		 /usr/lib/freetz/libwrap.so.0.7.6
