--- xtables.c
+++ xtables.c
@@ -352,6 +352,8 @@
 		dir = next + 1;
 	} while (*next != '\0');
 
+	fprintf(stderr, "Extension (%s)%s not found in %s\n", prefix, name, search_path);
+
 	return NULL;
 }
 #endif
