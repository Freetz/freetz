CAPTION="RSnyc: $(lang de:"Experten Optionen" en:"advanced options")"
DESCRIPTION="$(lang de:"Optional. RSync typische Syntax" en:"Optional, RSync typical syntax")"

CONFIG_FILE='/tmp/flash/rsync/sharesx'
CONFIG_SAVE='modsave flash; if pidof rsync > /dev/null; then echo ""; /mod/etc/init.d/rc.rsync restart; fi'
CONFIG_TYPE='text'

TEXT_ROWS=10

