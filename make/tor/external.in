config EXTERNAL_FREETZ_PACKAGE_TOR
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_TOR
	bool "Tor"
	default n
	help
		externals the following file(s):
		 /usr/sbin/tor
		 /usr/share/tor/geoip (if included)
		 /usr/share/tor/geoip6 (if included)

