config EXTERNAL_FREETZ_PACKAGE_STREAMRIPPER
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_STREAMRIPPER && FREETZ_SHOW_EXPERT
	bool "streamripper"
	default n
	help
		externals the following file(s):
			/usr/bin/streamripper

