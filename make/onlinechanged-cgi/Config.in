config FREETZ_PACKAGE_ONLINECHANGED_CGI
	bool "Execution of scripts when IP address changes"
	default n
	help
		When the IP addresses changes during a forced re-connect
		this CGI allows you to specify arbitrary commands which are
		executed at that event. The following different change events
		are considered: initial start of the box, going online after
		forced re-connect, going offline.

