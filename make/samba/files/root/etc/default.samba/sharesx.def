CAPTION='Experten Optionen des Samba Servers'
DESCRIPTION='Optional. Samba typischer Syntax'

CONFIG_FILE='/tmp/flash/samba/sharesx'
CONFIG_SAVE='modsave flash; if [ -n "$(pidof smbd)" ]; then echo ""; /mod/etc/init.d/rc.samba restart; fi'
CONFIG_TYPE='text'

TEXT_ROWS=10
