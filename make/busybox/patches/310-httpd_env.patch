--- networking/httpd.c.orig	2007-11-04 00:06:35.000000000 +0100
+++ networking/httpd.c	2007-11-04 02:14:01.000000000 +0100
@@ -2103,7 +2103,7 @@
 	{
 		char *p = getenv("PATH");
 		/* env strings themself are not freed, no need to strdup(p): */
-		clearenv();
+		//clearenv();
 		if (p)
 			putenv(p - 5);
 //		if (!(opt & OPT_INETD))
