--- util-linux/getopt.c.orig	2008-06-25 14:51:32.000000000 +0200
+++ util-linux/getopt.c	2008-12-10 22:25:25.000000000 +0100
@@ -164,6 +164,14 @@
 	/* optreset = 1; */
 #endif
 
+	/* We used it already in getopt32(), we *must* reset getopt(3) */
+#ifdef __GLIBC__
+	optind = 0;
+#else /* BSD style */
+	optind = 1;
+	/* optreset = 1; */
+#endif
+
 	while (1) {
 		opt =
 #if ENABLE_GETOPT_LONG
