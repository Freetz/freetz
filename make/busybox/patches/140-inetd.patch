Revert change caused by bogus bug report
http://busybox.net/bugs/view.php?id=1562

--- networking/inetd.c	2007-11-09 18:40:47.000000000 -0700
+++ networking/inetd.c	2007-11-12 12:51:58.000000000 -0700
@@ -813,7 +813,7 @@
 		sep->se_bi = NULL;
 #endif
 	argc = 0;
-	for (; cp; arg = skip(&cp)) {
+	for (arg = skip(&cp); cp; arg = skip(&cp)) {
 		if (argc < MAXARGV)
 			sep->se_argv[argc++] = xxstrdup(arg);
 	}
