--- configure
+++ configure
@@ -4188,7 +4188,7 @@
 if test ${with_perl+y}
 then :
   withval=$with_perl;     if test "$withval" = "no"; then
-	if test -n ""; then
+	if test -n "allow-without"; then
 		PERL_PATH=$withval
 		{ printf "%s\n" "$as_me:${as_lineno-$LINENO}: Disabling use of PERL" >&5
 printf "%s\n" "$as_me: Disabling use of PERL" >&6;}
