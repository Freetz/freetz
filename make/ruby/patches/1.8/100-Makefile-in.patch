--- Makefile.in
+++ Makefile.in
@@ -131,7 +131,7 @@
 		if RUBY_PLATFORM =~ /mswin|bccwin|mingw/; \
 		  class File; \
 		    remove_const :ALT_SEPARATOR; \
-		    ALT_SEPARATOR = "\\"; \
+		    ALT_SEPARATOR = "\\\\"; \
 		  end; \
 		end; \
 		' > $@
