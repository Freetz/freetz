CAPTION='Quagga - ripd.conf'
DESCRIPTION="$(lang de:"Konfigurationsdatei f&uuml;r den RIP Daemon." en:"Config file for the RIP Daemon.")"

CONFIG_FILE='/tmp/flash/quagga/ripd.conf'
CONFIG_SAVE='modsave flash; if [ ! -z "$(pidof ripd)" ]; then echo ""; /mod/etc/init.d/rc.quagga restart; fi'
CONFIG_TYPE='text'

