--- linux-4.1/scripts/Makefile.headersinst
+++ linux-4.1/scripts/Makefile.headersinst
@@ -10,6 +10,12 @@
 # generated header directory
 gen := $(if $(gen),$(gen),$(subst include/,include/generated/,$(obj)))
 
+$(srctree)/$(obj):
+	mkdir -p $@
+
+$(srctree)/$(obj)/Kbuild: $(srctree)/$(obj)
+	touch $@
+
 kbuild-file := $(srctree)/$(obj)/Kbuild
 include $(kbuild-file)
 
