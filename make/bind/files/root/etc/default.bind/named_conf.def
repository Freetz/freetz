CAPTION='Bind: named.conf'

CONFIG_FILE='/tmp/flash/bind/named.conf'
CONFIG_SAVE='modsave flash; if pidof named > /dev/null; then echo ""; /mod/etc/init.d/rc.bind restart; fi'
CONFIG_TYPE='text'

TEXT_ROWS=40
