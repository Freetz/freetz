config EXTERNAL_FREETZ_PACKAGE_SIPROXD
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_SIPROXD
	bool "siproxd"
	default n
	help
		externals these file(s):
		 /usr/bin/siproxd
		 /usr/lib/freetz/siproxd (only if external option "Keep subdirectories" is enabled)
