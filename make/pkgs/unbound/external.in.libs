config EXTERNAL_FREETZ_LIB_libunbound
	depends on EXTERNAL_ENABLED && FREETZ_LIB_libunbound
	bool "libunbound"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/libunbound.so.8.1.20

