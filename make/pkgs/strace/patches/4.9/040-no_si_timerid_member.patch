--- signal.c
+++ signal.c
@@ -447,7 +447,7 @@
 				printsigsource(sip);
 				break;
 #endif
-#ifdef SI_TIMER
+#if 0 // #ifdef SI_TIMER
 			case SI_TIMER:
 				tprintf(", si_timerid=%#x, si_overrun=%d",
 					sip->si_timerid, sip->si_overrun);
