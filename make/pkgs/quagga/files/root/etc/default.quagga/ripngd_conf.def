CAPTION='Quagga - ripngd.conf'
DESCRIPTION="$(lang de:"Konfigurationsdatei f&uuml;r den RIPng Daemon." en:"Config file for the RIPng Daemon.")"

CONFIG_FILE='/tmp/flash/quagga/ripngd.conf'
CONFIG_SAVE='modsave flash; if [ ! -z "$(pidof ripngd)" ]; then echo ""; /mod/etc/init.d/rc.quagga restart; fi'
CONFIG_TYPE='text'

