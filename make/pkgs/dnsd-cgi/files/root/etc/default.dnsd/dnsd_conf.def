CAPTION='dnsd.conf'

CONFIG_FILE='/tmp/flash/dnsd/dnsd.conf'
CONFIG_SAVE='modsave flash; if pidof dnsd > /dev/null; then echo ""; /mod/etc/init.d/rc.dnsd restart; fi'
CONFIG_TYPE='text'

TEXT_ROWS=20
