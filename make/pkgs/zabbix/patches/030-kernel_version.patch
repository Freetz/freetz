--- configure
+++ configure
@@ -7499,7 +7499,7 @@
 	{ $as_echo "$as_me:${as_lineno-$LINENO}: checking for the linux kernel version" >&5
 $as_echo_n "checking for the linux kernel version... " >&6; }
 
-	kernel=`uname -r`
+	kernel="2.6.x" # all Fritz!Boxes supported by freetz are based on kernel 2.6.x
 
 	case "${kernel}" in
 		2.6.*)
