config FREETZ_PACKAGE_AUTOSSH
	bool "autossh 1.4g (binary only)"
	default n
	help
		autossh is a program to start a copy of ssh and monitor it,
		restarting it as necessary should it die or stop passing traffic.

