config EXTERNAL_FREETZ_PACKAGE_MTR
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_MTR
	bool "mtr"
	default n
	help
		externals the following file(s):
		 /usr/sbin/mtr
		 /usr/sbin/mtr-packet (if available)

