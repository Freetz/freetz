--- modules/Makefile.in
+++ modules/Makefile.in
@@ -180,7 +180,7 @@
 		echo $(INSTALL_DATA) $$i $(DEST_SLSH_DIR)/$$d/; \
 		$(INSTALL_DATA) $$i $(DEST_SLSH_DIR)/$$d/; \
 	   done; \
-        done
+	done
 	-$(MKINSDIR) $(DEST_CMAPSDIR)
 	@for i in cmaps/*.map; \
 	do \
