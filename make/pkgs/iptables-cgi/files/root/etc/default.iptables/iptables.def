CAPTION='Rules (iptables)'
DESCRIPTION='Diese ACCESS Liste wird beim Starten des Dienstes geladen.'
CONFIG_FILE='/tmp/flash/iptables_rules'
CONFIG_SAVE='modsave flash; lsmod | grep "ip_tables" > /dev/null; if [ $? -eq 0 ]; then echo ""; /mod/etc/init.d/rc.iptables restart; fi'
CONFIG_TYPE='text'
