config EXTERNAL_FREETZ_LIB_liblua
	depends on EXTERNAL_ENABLED && FREETZ_LIB_liblua && !FREETZ_PACKAGE_HASERL_WITH_LUA
	bool "liblua"
	default n
	help
		externals the following file(s):
		 ${FREETZ_LIBRARY_DIR}/liblua.so.x.x.x

