--- src/config.h.orig	2009-11-20 18:14:42.000000000 +0100
+++ src/config.h	2009-11-20 18:15:57.000000000 +0100
@@ -37,7 +37,7 @@
 #else
 #  define RESOLVFILE "/etc/resolv.conf"
 #endif
-#define RUNFILE "/var/run/dnsmasq.pid"
+#define RUNFILE "/var/run/dnsmasq/dnsmasq.pid"
 
 #ifndef LEASEFILE
 #   if defined(__FreeBSD__) || defined (__OpenBSD__) || defined(__DragonFly__) || defined(__NetBSD__)
@@ -45,7 +45,7 @@
 #   elif defined(__sun__) || defined (__sun)
 #      define LEASEFILE "/var/cache/dnsmasq.leases"
 #   else
-#      define LEASEFILE "/var/lib/misc/dnsmasq.leases"
+#      define LEASEFILE "/var/tmp/dnsmasq.leases"
 #   endif
 #endif
 
@@ -53,7 +53,7 @@
 #   if defined(__FreeBSD__)
 #      define CONFFILE "/usr/local/etc/dnsmasq.conf"
 #   else
-#      define CONFFILE "/etc/dnsmasq.conf"
+#      define CONFFILE "/mod/etc/dnsmasq.conf"
 #   endif
 #endif
 
@@ -197,9 +197,9 @@
 #endif
 #undef HAVE_ARC4RANDOM
 #undef HAVE_SOCKADDR_SA_LEN
-#if !defined(__ARCH_HAS_MMU__) && !defined(__UCLIBC_HAS_MMU__)
-#  define NO_FORK
-#endif
+//#if !defined(__ARCH_HAS_MMU__) && !defined(__UCLIBC_HAS_MMU__)
+//#  define NO_FORK
+//#endif
 #if defined(__UCLIBC_HAS_IPV6__)
 #  ifndef IPV6_V6ONLY
 #    define IPV6_V6ONLY 26
