config EXTERNAL_FREETZ_PACKAGE_TRICKLE
	depends on EXTERNAL_ENABLED && FREETZ_PACKAGE_TRICKLE
	bool "trickle"
	default n
	help
		externals these file(s):
		 /usr/bin/trickle
		 /usr/bin/trickled
		 /usr/bin/tricklectl
		 /usr/lib/freetz/trickle-overload.so
