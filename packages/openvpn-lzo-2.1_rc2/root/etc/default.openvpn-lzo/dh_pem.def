CAPTION='Diffie-Hellmann Parameter'
DESCRIPTION='$(lang de:"Wird nur für den Server-Betrieb mit Zertifikaten benötigt." en:"Only needed if run as Server with certficiates.")'
CONFIG_FILE='/tmp/flash/dh.pem'
CONFIG_SAVE='modsave flash; if [ ! -z "$(pidof openvpn)" ]; then echo ""; /mod/etc/init.d/rc.openvpn-lzo reload; fi'
CONFIG_TYPE='text'
