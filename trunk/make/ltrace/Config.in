config DS_PACKAGE_LTRACE
	bool "ltrace 0.5, svn-77 (binary only)"
	default n
	select DS_LIB_libelf
	help
		ltrace - library call tracer

		ltrace is a debugging tool, similar to strace, but it traces library
		calls instead of system calls.
