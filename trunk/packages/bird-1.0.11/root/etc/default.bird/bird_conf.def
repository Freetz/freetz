CAPTION='bird.conf (BIRD)'
DESCRIPTION='$(lang de:"Konfigurationsdatei für den BIRD Internet Routing Daemon." en:"Config file for the BIRD Internet Routing Daemon.")'
CONFIG_FILE='/tmp/flash/bird.conf'
CONFIG_SAVE='modsave flash; if [ ! -z "$(pidof bird)" ]; then echo ""; /mod/etc/init.d/rc.bird restart; fi'
CONFIG_TYPE='text'
