--- bfd/elf32-arm.c.orig	2010-05-27 20:26:45.000000000 +0200
+++ bfd/elf32-arm.c	2010-06-02 11:09:16.790403301 +0200
@@ -5606,6 +5606,10 @@
   if (! is_arm_elf (abfd))
     return;
 
+  /* PR 7093: Make sure that we are dealing with an arm elf binary.  */
+  if (! is_arm_elf (abfd))
+    return;
+
   if ((abfd->flags & DYNAMIC) != 0)
     return;
 
