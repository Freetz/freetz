--- fitimg
+++ fitimg
@@ -185,6 +185,7 @@
 	version() if $ARGV[0] =~ /^(-v|--version)$/;
 	release() if $ARGV[0] eq '--release'; # [--public] create release archive - BUMP $VERSION IN SECOND LINE AFTER!
 }
+else { help() }
 
 # read the arguments: -l -t -x -r -c -s -h -o -d -p require an argument, -i -n -f -e -k -m -u and -q do not
 my %opts;
