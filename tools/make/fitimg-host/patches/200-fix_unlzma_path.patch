--- fitimg
+++ fitimg
@@ -28,8 +28,8 @@
 my $unlzma;
 if (defined($ENV{'TOOLS_DIR'})) { # freetz host tools
 	$unlzma = "$ENV{'TOOLS_DIR'}/unlzma";
-} elsif (open(WHICH, "-|", "which", "unlzma")) { 
-	$unlzma = <WHICH>; 
+} elsif (open(WHICH, "-|", "which", "bash")) { 
+	$unlzma = "./unlzma";
 	close WHICH; 
 	chomp $unlzma;
 }
