--- etc/hotplug/run_mount	2006-12-09 01:13:02.000000000 +0100
+++ etc/hotplug/run_mount	2006-12-09 15:13:33.000000000 +0100
@@ -51,7 +51,7 @@
 	fi
 	
 	# Build nice name: [Product/Vendor name]-Partition-<x>-<y>
-	NICE="$NAME-Partition-$2-$3"
+	NICE="uStor$2$3"
 	echo $NICE
 }
 
@@ -92,7 +92,7 @@
 	test -d $MNTPATH || mkdir -p $MNTPATH
 	
 	passeeren
-	if mount -t vfat -o $READMODE,uid=$FTPUID,gid=$FTPGID $DEVNODE $MNTPATH; then
+	if mount -t vfat -o $READMODE,uid=$FTPUID,gid=$FTPGID,fmask=0000,dmask=0000 $DEVNODE $MNTPATH; then
 		OK=0
 		eventadd 140 $MNTNAME
 		if ! pidof ftpd; then
