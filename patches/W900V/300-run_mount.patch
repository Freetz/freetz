--- etc/hotplug/run_mount~
+++ etc/hotplug/run_mount
@@ -94 +94 @@
-       if mount -t vfat -o $READMODE,uid=$FTPUID,gid=$FTPGID $DEVNODE $MNTPATH; then
+       if mount -o $READMODE,sync,uid=$FTPUID,gid=$FTPGID $DEVNODE $MNTPATH; then